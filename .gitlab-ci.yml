stages:
  - build
  - test

build:
  stage: build
  script:
    - echo "this is building on $(hostname)"
    - mkdir builds
    - touch builds/data.txt
    - echo "false" > builds/data.txt
  artifacts:
    paths:
      - builds/

test:
  stage: test
  script:
    - echo "this is testing on $(hostname)"
    - test -f builds/data.txt
    - grep "true" builds/data.txt
